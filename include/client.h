#ifndef CLIENT_H
#define CLIENT_H

// Start the client and do the login on the server
int start_client(const char *addr, unsigned int port);

// Send a message to the server
int send_message(/** TODO : Define the message structure */)

#endif